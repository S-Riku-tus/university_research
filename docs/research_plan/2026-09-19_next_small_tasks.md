# 9/18発表後：次に行う小タスク

2026-09-19作成。研究の基準は[9/18最終スライドの計画](2026-09-18_presented_master_plan.md)、目的と達成条件は[発表後の研究判断](2026-09-19_research_actions.md)。以下の表は**作成時の着手順の提案**であり、実施状況は各節の追記を参照する。現行の実験日設定には編集中の変更があるため、[9/18完了run](../../experiments/2026-09-18_onb_full_sweep_151715/README.md)と混同しない。各新規比較は、開始前に`configs/`へ条件、終了後に`experiments/YYYY-MM-DD_.../`へ結果・評価単位・判断を保存する。

## 最初に行う：再学習を必要としない診断

| ID | 小タスク（入力→操作） | 残す成果物・完了条件 |
|---|---|---|
| A1 | [保存予測の監査](../../experiments/2026-09-19_noise_recovery_review/README.md)を開き、対象runの学習日6/11・評価日6/18・22 kHz・matched・seed 42・全1080秒を1行の条件表に写す | 対象を固定した条件表。別run・別日を混ぜていない |
| A2 | 無雑音と−8 dBの保存予測から、同じ`WAV ID×秒`の正解、RF、CNN＋Transformer、AlexNet、統合、重みを突き合わせる | 欠損・重複・ラベル不一致数を記した対応表。予測が保存されていない列は「未保存」とする |
| A3 | 各秒で`予測−正解`、二乗誤差、ONB閾値をまたぐ見逃し・誤警報を計算する。重みはその条件で実際に使った値を読む | 単体と統合の符号付き残差表。閾値の出典・単位も記す |
| A4 | ONB前・近傍・ONB以上に分け、各領域の秒数、WAV数、二乗誤差和・近傍RMSE、見逃し・誤報を比較する | 統合の得失がどの領域にあるか1枚の表。近傍幅は計算前に固定し、各WAVの60秒を独立実験60本として扱わない |
| A5 | 改善・悪化の大きい秒をWAVごとに抽出し、単体残差の符号と大きさ、`(Σw e)²`の対角・交差項を確認する | 「相殺／同方向の誤差／重みで増幅」のどれが実際に起きたか、代表秒と全体集計を併記 |
| A6 | 同じ保存予測で現行統合と等重みを比較する。テスト正解から重みを再fitしない | 重み自体が問題か、単体予測自体が問題かを分けた暫定判断。等重みは事後診断と明記 |
| A7 | −8→−16／−20 dBの[WAV別二乗誤差差](../../experiments/2026-09-19_noise_recovery_review/paired_wav_sse_changes.csv)を並べ、大きい改善・悪化WAVと熱流束領域を特定する | ノイズ曲線の回復が一部WAVに集中するか、事実と未識別原因を分離した短いメモ |

**Aの停止条件**：悪化を担うモデル・領域・WAVが特定でき、次に区別する原因を1〜2個に絞れたら終了。再計算で原因の断定や新しいXAI図の量産はしない。

**2026-09-19実施状況**：A1〜A7を[保存予測の標本別診断](../../experiments/2026-09-19_noise_recovery_review/A1_A7_saved_prediction_diagnosis.md)で完了。閾値の原記録との不一致は未解決として記録。次の原因識別はBの固定モデル対照。

## 次に行う：ノイズ谷の原因を切り分ける

| ID | 小タスク | 残す成果物・完了条件 |
|---|---|---|
| B1 | `clean_only`の実装経路、保存モデル・PCA・scaler・重みの有無、ノイズ配列の対応を確認する | 「既存学習済み状態を再利用可／再学習が必要」を明記した実行可否表。現在編集中の設定は完了runから分離 |
| B2 | まず無雑音、−8、−16、−20 dBを代表条件として**実行前**に記録。学習日・評価日、全1080秒、評価指標、seed、モデル・前処理・重み固定規則を条件ファイルに固定する | `configs/`の条件書。−8等は既存結果から選んだ探索的条件と明記 |
| B3 | 同じ無雑音学習済みモデルと前処理・重みで4条件を予測する。未保存なら同一seedのclean学習を1回だけ行ってから全SNRへ適用 | 予測とログ。SNRごとに再学習していないことが確認できる |
| B4 | A3〜A7の表と同じ標本キー・指標で、固定モデルの曲線、無雑音からの差、ONB誤りを計算する | 谷が残るか否かを判定。**消失**ならmatched再学習・重み変動を次に検査、**残存**なら入力・ノイズ・日差への感度を次に検査。これだけで単一原因と断定しない |
| B5 | 代表的な2条件について、学習loss・乱数設定・前処理fit範囲・実行順を照合する。既存図はtraining lossのみと記す | 学習側で確認できた事実と不明点の一覧。validation loss／best epochを既存runにあるかのように報告しない |
| B6 | B4で残った不明点に絞り、**同一条件の複数seed（まず3程度）**を対応比較する。必要なら学習日内で元WAVを分離したvalidationを設ける | seed差とSNR差の相対規模、学習曲線。評価日6/18をepoch選択に使わない |
| B7 | 固定モデルでも谷が残れば、同じWAV・秒・水流ノイズ片についてHz帯別の実測SNRと、保持／除去摂動での予測変化を照合する | 特定の帯域・ノイズ源に依存するかの識別結果。帯域幅、ゼロ置換の分布外影響、resizeの変化を明記 |

B1〜B4はノイズ問題の優先実験。B5は並行できる。B6・B7はB4の結果に応じて範囲を絞る。既存matchedの谷を滑らかな曲線に修正することは目標にしない。

**2026-09-19実施状況**：B1〜B6を[固定モデル・複数seedの実験記録](../../experiments/2026-09-19_b_clean_only/README.md)で完了。固定cleanモデルでは谷型の回復が消え、−8→−16 dBのmatched統合R²差は3 seedで正・負に分かれた。B7は「固定モデルでも谷が残れば」という開始条件を満たさず未実施。次は学習日内の元WAV分離validationでepoch別の未知WAV誤差とONB誤りを記録し、選別の有無はCの対応比較で扱う。

## 基準条件とアンサンブルを決める

| ID | 小タスク | 残す成果物・完了条件 |
|---|---|---|
| C1 | ピーク選別の**なし／あり**で、学習6/11→評価6/18、22 kHz、無雑音、学習予算、seed群、テスト全1080秒を同じにした条件書を作る | 変更変数が選別のみの比較表。除外数と更新回数の記録方法を指定 |
| C2 | 条件書どおり対応するseedで実行し、全域R²/MAE、ONB近傍RMSE、見逃し・誤報、WAV別差を比較する | 「採用／不採用／未確定」の判定。改善なら同数ランダム除外を追加し、データ数効果と音響基準を分ける |
| C3 | A・B・C2を見て、現行重みと等重みのどちらを基準にするか学習側で決める。共通誤差が主因なら重み方式追加は保留する | 採用方式と失敗条件。テストで最良だった方式を事後的な採用根拠にしない |
| C4 | 別実験日を独立評価に使えるか、同期した気泡観察等の物理正解があるか、教授に確認する | 取得可否と、取得できない場合の主張の限界。準備はA・Bと並行可 |

**2026-09-20実施状況**：C1〜C3を[選別なし／ありの3 seed対応比較](../../experiments/2026-09-20_c_selection/README.md)で完了。run閾値での1e-9選別はONB前の過大予測を抑える一方、近傍1 WAV・ONB以上の誤差を全seedで増やし、基準には不採用。現行`inner_holdout`重みを運用上の基準として維持し、等重みは固定対照とする。C4は本人確認により未使用の独立日・音声同期映像が現時点でないことを記録し、教授へ確認する質問を準備した。**教授への直接確認とONB閾値の定義確定は未了**。

## 説明性と修論を進める

| ID | 小タスク | 残す成果物・完了条件 |
|---|---|---|
| D1 | Aで決めた成功・失敗の同一秒について、[手法選択メモ](../notes/2026-09-19_explainability_method_selection.md)を参照し、帯域マスク結果と残差を並べる | どの帯域感度がどの**誤り**に対応したか、対応しなかったかを記録 |
| D2 | RFはTreeSHAPの予測再構成、深層はIGの数値収束・baseline感度、AlexNetのGrad-CAMは層・粗さ・正方向限定を点検する | 信頼できない地図を主張から外した説明性判定表。別のCAM系は、粗さが研究判断を妨げる場合だけ1方式を比較 |
| D3 | マスクの示唆が入力改善につながる場合にだけ、同幅対照・代替置換・必要なら帯域保持／除去再学習を計画する | 帯域仮説を採用・棄却する条件。図の見た目で物理的気泡音と断定しない |
| D4 | 10月から背景・実験条件・評価設計・手法選択理由を書く。A〜Cの判断ごとに結果・限界を追記する | 各主張がrun条件と図表へ遡れる草稿。12月に主結果を実質固定する目安、正式日程は別途確認 |

**作成時の着手点**はA1→A2→A3だった。A〜Cの実施状況は上記の追記を参照。次は教授とONB閾値の定義・独立日/同期観察の取得可否を確認し、学習日内の元WAV分離validationでepoch別の未知WAV誤差を診断する。研究の達成は、統合が全条件で勝つことではなく、条件ごとの得失とONBへの効き方を、分割・反復・説明性の限界を含めて説明できること。
